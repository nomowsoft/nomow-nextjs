export type MenuItem = {
    name: string;
    href: string;
    isActive: boolean;
};
export type Aboutus = {
    id: number;
    title: string;
    description: string;
    image: string;
};
export type OurService = {
    id: number;
    title: string;
};
